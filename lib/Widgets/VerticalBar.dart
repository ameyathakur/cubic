import 'package:flutter/material.dart';

class VerticalBar extends StatelessWidget {
  const VerticalBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: double.infinity,
      width: 73.0,
      color: Color(0xFFFFBD59),
    );
  }
}
